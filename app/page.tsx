export default function Home() {
  // const launchDate = new Date(Date.now() + 60 * 60 * 1000);
  return (
    <main className="relative w-full h-screen bg-white flex flex-col items-center justify-center overflow-hidden">
      <span className="text-[#BC002D] font-['Electroharmonix'] text-xl tracking-[0.8em]">
        SATHWA'26
      </span>
      <span className="text-gray-400 font-sans text-sm tracking-widest uppercase">
        CEM
      </span>
    </main>
  );
}
